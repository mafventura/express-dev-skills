<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel='stylesheet' href='/stylesheets/style.css' />
        <title>Skills List</title>
    </head>
    <body>
        <nav>
            <ul>
            <li><a href="/">&lt; Back</a></li>
            </ul>
        </nav>

        <ul>
            <% skills.forEach(el => { %>
                <li>
                    <a href="/skills/<%= el.id %>"><%= el.name %></a>
                </li>
            <% }); %>
        </ul>
    </body>
</html>
